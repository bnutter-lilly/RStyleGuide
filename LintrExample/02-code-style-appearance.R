# Assignment

x <- "This is correct"
y = "This will return a lint"

# Braces

if (TRUE) { 
  "This is considered Correct"
}

if (TRUE)
{
  "But with the braces on their own line it produces a lint"
}
