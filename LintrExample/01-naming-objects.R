CONSTANT <- "Value"

# The only naming convention we explicitly disallow is using the dot (.)
# in variable names.

this.value <- "abc"

# An object name with too many characters

objects_names_should_have_less_than_thirty_two_characters <- FALSE
